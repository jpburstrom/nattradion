thisProcess.nowExecutingPath.debug("TODO");

Cell({

	~template[\buffer] = Fdef(\resolveAssets).value((
		 '128_höjning': "128 höjning/128 höjning.wav" 
	));

}, \disk).name_(PathName(thisProcess.nowExecutingPath).folderName)
